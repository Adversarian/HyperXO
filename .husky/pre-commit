cd frontend || exit 0
echo "Running type check..."
npx tsc -b --noEmit || { echo "Type check failed."; exit 1; }
echo "Running linter..."
npx eslint . --max-warnings 0 || { echo "Lint failed."; exit 1; }
echo "All checks passed."
